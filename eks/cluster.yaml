apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: fitness-eks
  region: us-east-2

managedNodeGroups:
  - name: workers
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
